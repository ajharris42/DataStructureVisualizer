#include "GraphicTreeNode.h"
/*
GraphicTreeNode::GraphicTreeNode(int x, int y, int w, int h, int value)
{
    left = NULL;
    right = NULL;

    this->x = x;
    this->y = y;
    this->w = w;
    this->h = h;

    this->value = value;
}

void GraphicTreeNode::displayNode()
{
    glColor3f(1.0f, 0.0f, 0.0f);
    glVertex2f(x, y);
    glVertex2f(x + w, y);
    glVertex2f(x + w,  y + h);
    glVertex2f(x,  y + h);
}

GraphicTreeNode::~GraphicTreeNode()
{

}

*/
